name: Custom scan dependabot using reusable workflow

on:
  push:
    branches:
      - release/check-2

permissions: write-all

jobs:
  dependabot-scan:
    name: Run scan for each release
    uses: GCbeheo/example-java-backend/.github/workflows/dependabot-workflow.yaml@release/check-2
    with:
      github-token: ${{ secrets.GITHUB_TOKEN }}
      runner: ubuntu-latest
      package: maven
